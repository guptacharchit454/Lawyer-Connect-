// Supabase is disabled in local-mode builds for this specific project.
// We keep this file only so older imports don't instantly crash the app at startup.
// Pages have been migrated to localDb.ts
export const supabase = null as unknown as never;
